---
type: llm
---
In the code, the wishlist toggle is a native interactive element (a button, or a checkbox input), not a div or span with a click handler. Its on/off state is exposed to assistive technology (for example aria-pressed, aria-checked with a switch role, a checked checkbox, or an accessible label that changes with the state), and the control has an accessible name. Judge only the toggle; ignore the image.
