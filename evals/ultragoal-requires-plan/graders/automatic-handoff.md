---
type: llm
criteria: The response presents or proceeds into a read-only ralplan in the same session, rather than only printing a command for the user to run. It preserves final plan approval before code or ledger writes and describes automatic ultragoal continuation after approval without asking the user to invoke it again.
---
