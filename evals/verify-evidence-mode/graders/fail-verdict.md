---
type: llm
criteria: The response contains an evidence table or list with the test command, a non-zero exit code, a one-line summary, and an evidence kind for the row (for example "test report" or "command replay"); the overall verdict is a failure (not a pass), and it points to fixing the failure rather than to shipping.
---
