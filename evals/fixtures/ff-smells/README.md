# ff-smells fixture

A small Next.js-shaped shop project used by the `ff-*` eval cases. Three files carry deliberate smells (`OrderCard.tsx`, `ProductList.tsx`, `useCheckout.ts`), `PriceTag.tsx` is deliberately clean, and `scripts/sync-orders.mjs` plus the orders route handler are non-component code for the guard cases.
