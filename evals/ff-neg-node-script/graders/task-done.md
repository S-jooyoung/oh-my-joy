---
type: llm
---
The answer shows the complete sync-orders script with the nesting flattened through guard clauses or early returns/continues, and it keeps the original behavior: the same usage error and exit code, the same "input must be a JSON array" error, the same skip warnings, and the same synced output shape. It gives no React, component, or accessibility advice.
