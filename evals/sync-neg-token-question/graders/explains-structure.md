---
type: llm
---
The answer explains in Korean, based on `src/tokens/colors.css`, that the color tokens have two layers: primitive tokens holding raw hex values (such as --color-gray-900 or --color-blue-500) and semantic tokens (such as --color-fg-primary, --color-brand-primary, --color-feedback-error) that reference primitives through var(). It names at least two real tokens from the file. It does not report drift against Figma and does not propose running a sync.
